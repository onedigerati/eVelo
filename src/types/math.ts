/**
 * Math type definitions for statistical calculations
 */

/** Result of a statistical calculation with sample size context */
export interface StatisticalResult {
  value: number;
  sampleSize: number;
}
